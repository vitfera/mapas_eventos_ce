# Aumentar timeout do PHP (aplicado via php.ini)
# TimeOut e ProxyTimeout devem ser configurados no Apache global

# Habilitar rewrite
RewriteEngine On

# Cache control para assets
<FilesMatch "\.(js|css)$">
  Header set Cache-Control "no-cache, must-revalidate"
</FilesMatch>

